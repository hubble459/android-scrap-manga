<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/refresh"
        android:icon="@drawable/ic_refresh_24px"
        android:onClick="refresh"
        android:title="@string/refresh"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/auto_refresh"
        android:checkable="true"
        android:onClick="toggleAutoRefresh"
        android:title="@string/auto_refresh"
        app:showAsAction="never" />

    <item
        android:id="@+id/delete_manga"
        android:onClick="deleteManga"
        android:title="@string/delete_manga"
        app:showAsAction="never" />
</menu>